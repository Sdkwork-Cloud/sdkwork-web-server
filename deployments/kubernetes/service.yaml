apiVersion: v1
kind: Service
metadata:
  name: sdkwork-web-standalone-gateway
  labels:
    app.kubernetes.io/name: sdkwork-web
    app.kubernetes.io/component: standalone-gateway
spec:
  selector:
    app.kubernetes.io/name: sdkwork-web
    app.kubernetes.io/component: standalone-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
