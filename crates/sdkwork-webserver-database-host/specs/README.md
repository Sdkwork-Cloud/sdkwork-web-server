# sdkwork-webserver-database-host Specs

`component.spec.json` declares the Web database lifecycle adapter and its
SDKWork database-framework dependency. Database contract, baseline, seed, and
drift authority remains under `../../../database/`.

