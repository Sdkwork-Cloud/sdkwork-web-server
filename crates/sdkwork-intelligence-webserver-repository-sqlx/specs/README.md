# sdkwork-intelligence-webserver-repository-sqlx Specs

`component.spec.json` declares the SQLx adapter for `WebRepositoryPort`, its
database pool dependency, and dual-engine pagination/transaction verification.
The physical database contract remains under `../../../database/`.

