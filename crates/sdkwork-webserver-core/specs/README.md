# sdkwork-webserver-core Specs

- `component.spec.json` declares the component identity, public compiler port, dependencies, and verification.
- Root machine contract: `../../../specs/sdkwork.webserver.config.schema.json`.

Global standards remain authoritative under `../../../sdkwork-specs/` from the repository root.

