# SDKWork Web Server Route Common Specs

`component.spec.json` owns the shared HTTP problem, response-envelope, correlation, and fail-closed IAM adapter contract used by both route surfaces.

The crate owns no routes or generated SDKs. Verify from the repository root with the crate tests and strict component-port validation.
